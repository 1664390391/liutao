<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- 分散配置 -->
    <!--<context:property-placeholder location="classpath:springstudy.myspring/dispatch/db.properties,springstudy.myspring/dispatch/db2.properties" />-->

    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <value>classpath:springstudy/myspring/dispatch/db2.properties</value>
        </property>
    </bean>

    <!-- 配置bean -->
    <bean id="dbutil" class="springstudy.myspring.dispatch.DBUtil">
        <property name="drivername" value="${db2.drivername}"/>
        <property name="url" value="${db2.url}"/>
        <property name="username" value="${db2.username}"/>
        <property name="password" value="${db2.password}"/>
     </bean>

</beans>